#include <stdio.h>

int main()
{
    int a;
    scanf("%d",&a);
    if(a>=18)
    {
        printf("elligble for voting");
    }
else{
    printf(" not elligble for voting ");
}
return 0;
}